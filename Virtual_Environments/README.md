# Virtual Environments
This folder contains all virtual environments used in the shared control cataract project. The txt files contains lists of required package which can be read by pip
```
pip install -r src/Virtual_Environments/assistive_ff.txt
```